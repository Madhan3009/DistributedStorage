version: "3.9"

services:
  app:
    build: .
    ports:
      - "9000:8080"

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER : postgres
      POSTGRES_PASSWORD : admin
      POSTGRES_DB : user_db
    ports:
      - "5433:5432"
